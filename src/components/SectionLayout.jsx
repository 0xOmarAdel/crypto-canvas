"use client";
function SectionLayout({ children }) {
  return (
    <section
      style={{
        padding: "5rem 10rem",
      }}
    >
      {children}
    </section>
  );
}

export default SectionLayout;
